server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/cursosdb
    username: andres
    password: Campeon1!
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

